# One More Thing — Standard Closing Rule

Every skill MUST end its execution by applying this rule:

> If anything is unclear, missing, or conflicting, stop and ask the user before proceeding.

If a skill needs a domain-specific closing warning, it keeps its own `## One More Thing` text, but the standard rule still applies as the default.
